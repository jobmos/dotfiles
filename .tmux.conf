set -g status-bg black
set -g status-fg white

set -g prefix C-a
bind-key C-a last-window

set -g base-index 1

set -g mouse on

set -g renumber-windows on  # Automatically renumber windows when closing one
